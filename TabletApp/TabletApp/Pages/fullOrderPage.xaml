<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             BackgroundImageSource="baseBackground"
             x:Class="TabletApp.Pages.fullOrderPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label
                Text="ITEM"
                Margin="30, 10, 30, 10"
                FontSize="Medium"
                FontAttributes="Bold"
                TextColor="Black"
                Grid.Row="0"
                Grid.Column="0"/>

            <Label
                Text="PRICE"
                Margin="30, 10, 30, 10"
                FontSize="Medium"
                FontAttributes="Bold"
                TextColor="Black"
                Grid.Row="0"
                Grid.Column="1"/>

            <ScrollView
                x:Name="foOrderScroll"
                Grid.Row="1"
                Grid.RowSpan="2"
                Grid.Column="0"
                Grid.ColumnSpan="2">
                <StackLayout
                    x:Name="foFullOrderScroll">
                </StackLayout>
            </ScrollView>

            <Label
                x:Name="foFullOrderTotal"
                Text="ORDER TOTAL: $0.00"
                Margin="30, 10, 30, 10"
                FontSize="Medium"
                FontAttributes="Bold"
                TextColor="Black"
                Grid.Row="3"/>
            <Button
                x:Name="foPayTicket"
                Text="Pay Ticket"
                FontSize="Medium"
                TextColor="Black"
                Grid.Row="3"
                Grid.Column="1"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>